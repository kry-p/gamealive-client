import styled from 'styled-components';
import { backgroundColor } from '../lib/styles/global';

export const MainStyle = styled.div`
  height: 100vh;
  background-color: ${backgroundColor};
  position: relative;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  align-items: center;
`;
